using UnityEngine;

public class PlyToMesh : MonoBehaivour
{
    public static Mesh Load(string path)
    {
        //var ply = new PlyReader(path);
        //var mesh = new Mesh();
        //mesh.vertices = ply.Vertices;
        //mesh.triangles = ply.Triangles;
        //mesh.normals = ply.Normals;
        //mesh.colors = ply.Colors;
        //return mesh;
        return null;
    }
}